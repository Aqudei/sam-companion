<UserControl x:Class="SAM_COMPANION2.Views.CollectionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SAM_COMPANION2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Header="Import Data" Margin="4">
            <StackPanel Orientation="Horizontal">
                <Button Margin="2" x:Name="ImportStudents">Students</Button>
                <Button Margin="2" x:Name="ImportAttendance">Attendance Records</Button>
            </StackPanel>
        </GroupBox>
        <TabControl x:Name="Items" Grid.Row="1"/>
    </Grid>
</UserControl>
